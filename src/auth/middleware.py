[{'test@example.com': {'email': 'test@example.com', 'hashed_password': 'dummy_hash_for_testing'}}, ['str, Any]:\n    "', '\n    Dependency that validates JWT from Authorization header and returns user.\n    \n    Args:\n        credentials: HTTP Authorization credentials containing the JWT token\n        \n    Returns:\n        User dictionary if token is valid\n        \n    Raises:\n        HTTPException: If token is invalid or user not found\n    "', '\n    token = credentials.credentials\n    payload = verify_token(token)\n    \n    if not payload:\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail="Could not validate credentials', 'headers={"WWW-Authenticate": "Bearer'], {'WWW-Authenticate': 'Bearer'}, {'WWW-Authenticate': 'Bearer'}]